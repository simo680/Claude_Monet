  // // Определение связей между таблицами
  // Visitor.hasMany(Booking, { foreignKey: 'Visitor_ID' });
  // Booking.belongsTo(Visitor, { foreignKey: 'Visitor_ID' });
  
  // Booking.hasOne(Bill, { foreignKey: 'Booking_ID' });
  // Bill.belongsTo(Booking, { foreignKey: 'Booking_ID' });
  
  // Visitor.hasMany(Cart, { foreignKey: 'Visitor_ID' });
  // Cart.belongsTo(Visitor, { foreignKey: 'Visitor_ID' });
  
  // Cart.hasMany(Order, { foreignKey: 'Cart_ID' });
  // Order.belongsTo(Cart, { foreignKey: 'Cart_ID' });
  
  // Booking.hasOne(Tables, { foreignKey: 'Booking_ID' });
  // Tables.belongsTo(Booking, { foreignKey: 'Booking_ID' });
  
  // Employee.hasMany(Tables, { foreignKey: 'Employee_ID' });
  // Tables.belongsTo(Employee, { foreignKey: 'Employee_ID' });
  
  // Cart.hasMany(Menu, { foreignKey: 'Cart_ID' });
  // Menu.belongsTo(Cart, { foreignKey: 'Cart_ID' });
  

  